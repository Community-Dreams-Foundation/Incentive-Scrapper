requests>=2.31.0
beautifulsoup4>=4.12.0
pydantic>=2.5.0
playwright>=1.40.0

# Optional: only needed if you set USE_LLM_EXTRACTION=True and have an
# ANTHROPIC_API_KEY. The script runs fine without it.
# anthropic>=0.40.0
